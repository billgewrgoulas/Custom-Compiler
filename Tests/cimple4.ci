program awesome
    declare x, y, z;
    declare w;

    function f1(in a)

        declare z, i;

        procedure f2()
            declare count;

            function f3(inout b)
            {
                return(a + x + z);
            }
        
        {
            i:=0;
            while(i < 20)
            {
                if(i<10)
                {
                    print(i/2);
                }
                else if(i>10)
                {
                    print(2*i);
                }
                else
                {
                    print(f3(inout i));
                };;
                i:=1;
            };
            call f2();
        }

    {
        if (x<=(w+x))return(x);
        else return(w);
    }

    function f4()
    
        declare var;

        function f5(in a, inout b)

        {
            forcase
                case(a/2 = 0 or y*x<100)
                {
                    var:=1;
                }
                default
                {
                    var:=2;
                };
            return(f1(in var) + var);
        }

    {
        z:=2;
        w:=f5(in x, inout y);
        return(w);
    }
    
    {

        x := f1(in x);
        print(x);
    }.

       